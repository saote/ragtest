Modbus 接口 Copy
Flex-Modbus（UF-48VxxAMDN）
J1 & J2-Modbus
接口定义

以上媒体的描述: 图片中有两个插头的示意图。左边的是一个RJ45插头，右边的是一个RJ45插座。每个插头和插座上都有一个箭头指向一个特定的引脚位置，并标注为“1pin位置”。

以上媒体的描述: 图片中有两个网络端口，分别标记为“J1: Modbus In”和“J2: Modbus Out”。这些端口通常用于Modbus通信协议，J1端口用于Modbus输入，J2端口用于Modbus输出。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 |
| ------------ | ------------ | ---------- | ------------ |
| J1 & J2  | Modbus   | 1          | Not Used     |
|              |              | 2          | Not Used     |
|              |              | 3          | MODBUS_485A  |
|              |              | 4          | GND          |
|              |              | 5          | GND          |
|              |              | 6          | MODBUS_485B  |
|              |              | 7          | Not Used     |
|              |              | 8          | Not Used     |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下内容：

Modbus主站：
标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个匹配电阻（120Ω）。

接地（PE）。


Modbus节点：

每个节点标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
每个节点也接地（PE）。

节点1和节点2之间通过Modbus总线连接，节点2和节点n之间也通过Modbus总线连接。


连接线：

Modbus总线连接各个节点和主站。
接地线（PE）连接各个节点和主站。

这个示意图展示了如何通过Modbus总线将多个节点与主站连接起来，并且每个节点和主站都接地以确保通信的稳定性和可靠性。
J3-终端电阻的使用
| 接口编号 | 接口定义            | 说明                                     |
| ------------ | ----------------------- | -------------------------------------------- |
| J3           | Modbus 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
UF2-G48xxNDD-x General 常规型（CANopen or Modbus）
J1 & J2-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个连接器插座，标注了“Pin1”引脚的位置。这个插座有多个引脚，通常用于电子设备的连接。

以上媒体的描述: 图片中显示了一块电路板，上面有两个接口。左边的接口标记为“J1: CANopen / Modbus In”，右边的接口标记为“J2: CANopen / Modbus Out”。这些接口用于CANopen和Modbus通信协议的输入和输出。
| 接口编号 | 接口定义         | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | -------------------- | ---------- | ------------ | ------------ |
| J1 & J2  | CANopen / Modbus | 1          | MODBUS_485B  | 黑           |
|              |                      | 2          | MODBUS_485A  | 棕           |
|              |                      | 3          | GND          | 红           |
|              |                      | 4          | GND          | 橙           |
|              |                      | 5          | CANH         | 绿           |
|              |                      | 6          | CANL         | 黄           |
|              |                      | 7          | Not Used     | -            |
|              |                      | 8          | PE           | 屏蔽层       |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下内容：

CANopen主站：
标有“CANH”、“CANL”和“GND”的连接端口。
连接了一个120Ω的匹配电阻。

连接到PE（保护接地）。


CANopen节点1：

同样标有“CANH”、“CANL”和“GND”的连接端口。

连接到PE（保护接地）。


CANopen节点2：

同样标有“CANH”、“CANL”和“GND”的连接端口。

连接到PE（保护接地）。


CANopen节点n：

同样标有“CANH”、“CANL”和“GND”的连接端口。
连接了一个120Ω的匹配电阻。
连接到PE（保护接地）。

图中展示了各个节点之间的连接方式，CANH、CANL和GND分别通过线缆连接，形成一个完整的CANopen网络。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的示意图。图中包含以下元素：


Modbus主站：位于左侧，标有“Modbus 主站”。它连接到MODBUS_485A、MODBUS_485B和GND，并且有一个120Ω的匹配电阻。


Modbus节点：有多个节点，分别标为“Modbus 节点1”、“Modbus 节点2”和“Modbus 节点n”。每个节点都连接到MODBUS_485A、MODBUS_485B和GND。


PE（保护接地）：每个节点和主站都有一个PE连接。


通信线路：MODBUS_485A和MODBUS_485B通过通信线路连接，形成一个串行通信网络。


匹配电阻：在主站和最后一个节点（Modbus 节点n）上都有120Ω的匹配电阻。


这个示意图展示了一个典型的Modbus RS-485通信网络的连接方式，确保了各个节点之间的通信。
J7 & J3-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J7           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J3           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DS1-G48xxM-x  General 常规型（CANopen or Modbus）
J1 & J2-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个连接器插座的插针端视图。上方标有“Pin1”，指示第一个插针的位置。插座内部有多个金属插针，用于电气连接。

以上媒体的描述: 图片中显示了一块电路板的部分，标注了两个接口：

J1: CANopen / Modbus In
J2: CANopen / Modbus Out

这些接口用于连接CANopen或Modbus通信协议的设备。
| 接口编号 | 接口定义         | 引脚号 | 信号说明 |
| ------------ | -------------------- | ---------- | ------------ |
| J1 & J2  | CANopen / Modbus | 1          | MODBUS_485B  |
|              |                      | 2          | MODBUS_485A  |
|              |                      | 3          | GND          |
|              |                      | 4          | GND          |
|              |                      | 5          | CANH         |
|              |                      | 6          | CANL         |
|              |                      | 7          | Not Used     |
|              |                      | 8          | PE           |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：位于左侧，标有“CANopen 主站”。
CANopen节点：有三个节点，分别标为“CANopen 节点1”、“CANopen 节点2”和“CANopen 节点3”。
CAN总线连接：每个节点之间通过CAN总线连接，标有“CANH”、“CANL”和“GND”。
终端电阻：在主站和最后一个节点处，各有一个120欧姆的匹配电阻。
PE（保护接地）：每个节点和主站都有一个PE连接。

这个示意图展示了一个典型的CANopen网络拓扑结构，其中主站和各个节点通过CAN总线连接，并在两端使用终端电阻以匹配阻抗，确保信号完整性。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的示意图。图中包含以下内容：

Modbus主站：左侧的矩形框内标注为“Modbus主站”，其中包含：
MODBUS_485A
MODBUS_485B
GND
匹配电阻120Ω

PE（保护接地）


Modbus节点：右侧的多个矩形框内标注为“Modbus节点1”、“Modbus节点2”和“Modbus节点n”，每个节点包含：

MODBUS_485A
MODBUS_485B
GND
PE（保护接地）

节点1和节点n还包含匹配电阻120Ω


连接线：各节点之间通过线缆连接，表示Modbus通信线路。


这个示意图展示了一个典型的Modbus RS-485总线网络，其中主站和多个从站（节点）通过RS-485总线进行通信。每个节点都连接到总线上，并且在总线的两端使用匹配电阻以减少信号反射。
J7 & J3-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J7           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J3           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DS1-G48xxN-x  General 常规型（CANopen or Modbus）
J1 & J2-CANopen / Modbus
接口定义

以上媒体的描述: 图片中有两个插头的示意图。左边的是一个插头的正面视图，右边的是一个插座的正面视图。两个图中都有标注“1pin位置”，指示了插头和插座中第一个引脚的位置。

以上媒体的描述: 图片中显示的是一个电路板的一部分，包含两个以太网端口。端口上方有标签标识：

J1: CANopen / Modbus In
J2: CANopen / Modbus Out

这些标签表明这两个端口分别用于CANopen或Modbus协议的输入和输出。CANopen和Modbus是两种常见的工业通信协议。
| 接口编号 | 接口定义         | 引脚号 | 信号说明 |
| ------------ | -------------------- | ---------- | ------------ |
| J1 & J2  | CANopen / Modbus | 1          | Not Used     |
|              |                      | 2          | Not Used     |
|              |                      | 3          | MODBUS_485A  |
|              |                      | 4          | GND          |
|              |                      | 5          | GND          |
|              |                      | 6          | MODBUS_485B  |
|              |                      | 7          | CANH         |
|              |                      | 8          | CANL         |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：位于左侧，标有“CANopen 主站”。
CANopen节点：有多个节点，分别标有“CANopen 节点1”、“CANopen 节点2”和“CANopen 节点n”。
CAN总线连接：每个节点之间通过CAN总线连接，标有“CANH”、“CANL”和“GND”。
匹配电阻：在主站和最后一个节点处，各有一个120欧姆的匹配电阻，标有“匹配电阻120Ω”。
PE（保护接地）：每个节点和主站都有一个PE连接，标有“PE”。

这个示意图展示了一个典型的CANopen网络拓扑结构，其中主站和多个节点通过CAN总线连接，并在两端使用匹配电阻以确保信号完整性。
Modbus 主站线性拓扑结构

以上媒体的描述: 这是一张Modbus通信网络的示意图。图中展示了Modbus主站和多个Modbus节点之间的连接方式。具体内容如下：

Modbus主站：
包含MODBUS_485A、MODBUS_485B和GND接口。
连接了一个匹配电阻120Ω。

接地（PE）。


Modbus节点1：

包含MODBUS_485A、MODBUS_485B和GND接口。

接地（PE）。


Modbus节点2：

包含MODBUS_485A、MODBUS_485B和GND接口。

接地（PE）。


Modbus节点n：

包含MODBUS_485A、MODBUS_485B和GND接口。
连接了一个匹配电阻120Ω。
接地（PE）。

各节点之间通过MODBUS_485A、MODBUS_485B和GND接口进行连接，形成一个串行通信网络。每个节点都接地（PE），并且在主站和最后一个节点处连接了匹配电阻120Ω，以确保信号的完整性和稳定性。
J7 & J3-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J7           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J3           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DG1-G48xxx-x General 常规型（CANopen or Modbus or Pulse）
J2 & J6-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个连接器插座的插针端视图。上方标有“Pin1”，指示第一个插针的位置。这个连接器可能用于电子设备的电气连接。

以上媒体的描述: 图片显示了一个电路板的一部分，标注了两个连接器接口。左边的接口标注为“J6: CANopen / Modbus Out”，右边的接口标注为“J2: CANopen / Modbus In”。这些接口用于CANopen和Modbus通信协议的输入和输出。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | ------------ | ---------- | ------------ | ------------ |
| J2 & J6  | Modbus   | 1          | MODBUS_485B  | 黑           |
|              |              | 2          | MODBUS_485A  | 棕           |
|              |              | 3          | GND          | 红           |
|              | CANopen  | 4          | GND          | 橙           |
|              |              | 5          | CANH         | 绿           |
|              |              | 6          | CANL         | 黄           |
|              | **         | 7          | Not Used     | -            |
|              | 屏蔽**     | 8          | PE           | 屏蔽层       |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：左侧的矩形框内标注为“CANopen 主站”，并且有一个匹配电阻120Ω。
CANopen节点1：中间的矩形框内标注为“CANopen 节点1”。
CANopen节点2：右侧的矩形框内标注为“CANopen 节点2”。
CANopen节点：最右侧的矩形框内标注为“CANopen 节点”，并且有一个匹配电阻120Ω。

每个节点和主站都有以下连接：
- CANH：高电平CAN信号线
- CANL：低电平CAN信号线
- GND：地线
此外，每个节点和主站都有一个标注为“PE”的连接点。
图中还显示了CANH和CANL信号线之间的连接，表示它们在各个节点之间的通信。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的示意图。图中包含以下元素：


Modbus主站：左侧的矩形框内标注为“Modbus 主站”，并且有连接到“MODBUS_485A”、“MODBUS_485B”和“GND”的接口。还有一个标注为“匹配电阻120Ω”的组件。


Modbus节点：中间和右侧的矩形框分别标注为“Modbus 节点1”、“Modbus 节点2”和“Modbus 节点n”。每个节点都有类似的接口：“MODBUS_485A”、“MODBUS_485B”和“GND”。


PE（保护接地）：每个节点和主站都有一个标注为“PE”的组件，表示保护接地。


连接线：各个节点和主站之间通过连接线相连，表示通信线路。


这张图展示了一个典型的Modbus RS-485通信网络的连接方式，其中包括主站和多个从站（节点），并且在主站和最后一个节点上使用了匹配电阻以减少信号反射。
J1 & J5-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J1           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J5           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DG2-G48xxNDD-x General 常规型（CANopen or Modbus）
J1 & J2-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个连接器插座，标注了“Pin1”引脚的位置。这个插座有多个引脚，通常用于电子设备的连接。

以上媒体的描述: 图片中显示了一块电路板，上面有两个接口。左边的接口标记为“J1: CANopen / Modbus In”，右边的接口标记为“J2: CANopen / Modbus Out”。这些接口用于CANopen和Modbus通信协议的输入和输出。
| 接口编号 | 接口定义         | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | -------------------- | ---------- | ------------ | ------------ |
| J1 & J2  | CANopen / Modbus | 1          | MODBUS_485B  | 黑           |
|              |                      | 2          | MODBUS_485A  | 棕           |
|              |                      | 3          | GND          | 红           |
|              |                      | 4          | GND          | 橙           |
|              |                      | 5          | CANH         | 绿           |
|              |                      | 6          | CANL         | 黄           |
|              |                      | 7          | Not Used     | -            |
|              |                      | 8          | PE           | 屏蔽层       |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：左侧的矩形框内标有“CANopen 主站”，并且有一个标注为“匹配电阻120Ω”的组件。
CANopen节点1：中间的矩形框内标有“CANopen 节点1”。
CANopen节点2：右侧的矩形框内标有“CANopen 节点2”，并且有一个标注为“匹配电阻120Ω”的组件。
CAN总线连接：每个节点之间通过CAN总线连接，标有“CANH”、“CANL”和“GND”。
PE（保护接地）：每个节点都有一个标注为“PE”的组件，表示保护接地。

图中展示了CANopen网络的基本连接方式，包括主站和多个节点之间的连接，以及匹配电阻的使用。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下内容：

Modbus主站：
标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个匹配电阻120Ω。

接地标识“PE”。


Modbus节点1：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。

接地标识“PE”。


Modbus节点2：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。

接地标识“PE”。


Modbus节点n：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个匹配电阻120Ω。
接地标识“PE”。

各节点之间通过“MODBUS_485A”和“MODBUS_485B”进行连接，形成一个串行通信网络。每个节点的接地端（PE）也通过虚线连接在一起。
J7 & J3-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J7           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J3           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DG2-G48xxxDA-x General 常规型（CANopen or Modbus or Pulse）
J2 & J6-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个电子连接器的插座，通常用于电路板上的连接。插座上方标有“Pin1”，指示第一个引脚的位置。插座内部有多个金属引脚，用于与相应的插头连接。

以上媒体的描述: 图片中显示的是一个电路板的一部分，标注了两个接口。左边的接口标记为“J6: CANopen / Modbus Out”，右边的接口标记为“J2: CANopen / Modbus In”。这些接口用于CANopen和Modbus通信协议，分别用于输出和输入。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | ------------ | ---------- | ------------ | ------------ |
| J2 & J6  | Modbus   | 1          | MODBUS_485B  | 黑           |
|              |              | 2          | MODBUS_485A  | 棕           |
|              |              | 3          | GND          | 红           |
|              | CANopen  | 4          | GND          | 橙           |
|              |              | 5          | CANH         | 绿           |
|              |              | 6          | CANL         | 黄           |
|              | **         | 7          | Not Used     | -            |
|              | 屏蔽**     | 8          | PE           | 屏蔽层       |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包括以下部分：


CANopen 主站：左侧的第一个模块，标有“CANopen 主站”。它包含CANH、CANL和GND连接，以及一个120欧姆的匹配电阻。


CANopen 节点1：第二个模块，标有“CANopen 节点1”。它包含CANH、CANL和GND连接。


CANopen 节点2：第三个模块，标有“CANopen 节点2”。它包含CANH、CANL和GND连接。


CANopen 节点n：最右侧的模块，标有“CANopen 节点n”。它包含CANH、CANL和GND连接，以及一个120欧姆的匹配电阻。


PE（保护接地）：每个模块中都有一个标有“PE”的部分，表示保护接地。


连接线：CANH、CANL和GND通过连接线连接各个模块。


这个示意图展示了一个典型的CANopen网络拓扑结构，其中主站和多个节点通过CAN总线连接，并在总线的两端使用120欧姆的匹配电阻来终止总线。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下内容：

Modbus主站：
标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个匹配电阻120Ω。

接地（PE）。


Modbus节点1：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。

接地（PE）。


Modbus节点2：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。

接地（PE）。


Modbus节点n：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个匹配电阻120Ω。
接地（PE）。

各节点之间通过Modbus 485A和485B进行连接，形成一个串行通信网络。每个节点的接地（PE）也相互连接。
J1 & J5-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J1           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J5           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DP2-G48xxNDD-x General 常规型（CANopen or Modbus）
J1 & J2-CANopen / Modbus
接口定义

以上媒体的描述: 图片中是一个连接器插座的插针端视图。上方标有“Pin1”，指示第一个插针的位置。这个连接器插座有多个插针，通常用于电子设备的电气连接。

以上媒体的描述: 图片中显示了一块电路板，上面有两个标记为J1和J2的连接器。J1连接器标注为“CANopen / Modbus In”，表示这是一个用于CANopen或Modbus协议的输入端口。J2连接器标注为“CANopen / Modbus Out”，表示这是一个用于CANopen或Modbus协议的输出端口。电路板上还有其他一些电子元件和连接点。
| 接口编号 | 接口定义         | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | -------------------- | ---------- | ------------ | ------------ |
| J1 & J2  | CANopen / Modbus | 1          | MODBUS_485B  | 黑           |
|              |                      | 2          | MODBUS_485A  | 棕           |
|              |                      | 3          | GND          | 红           |
|              |                      | 4          | GND          | 橙           |
|              |                      | 5          | CANH         | 绿           |
|              |                      | 6          | CANL         | 黄           |
|              |                      | 7          | Not Used     | -            |
|              |                      | 8          | PE           | 屏蔽层       |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包括以下元素：

CANopen 主站：左侧的矩形框内标有“CANopen 主站”，并且有一个120欧姆的匹配电阻。
CANopen 节点1：中间的矩形框内标有“CANopen 节点1”。
CANopen 节点2：右侧的矩形框内标有“CANopen 节点2”。
CANopen 节点n：最右侧的矩形框内标有“CANopen 节点n”，并且有一个120欧姆的匹配电阻。

每个节点和主站之间通过三条线连接，分别标有“CANH”、“CANL”和“GND”。此外，每个节点和主站都有一个标有“PE”的接地点。
这些连接线和接地点共同构成了一个CANopen网络，用于工业自动化和控制系统中的通信。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下元素：

Modbus主站：位于左侧，标有“Modbus 主站”。
Modbus节点：有多个节点，分别标有“Modbus 节点1”、“Modbus 节点2”以及“Modbus 节点n”。
连接线：每个节点之间通过MODBUS_485A、MODBUS_485B和GND连接。
匹配电阻：在Modbus主站和最后一个Modbus节点上各有一个标有“匹配电阻120Ω”的电阻。
PE（保护接地）：每个节点和主站都有一个标有“PE”的接地连接。

这个示意图展示了一个典型的Modbus RS-485总线网络的连接方式，强调了信号线和接地线的连接，以及在总线两端使用匹配电阻以减少信号反射。
J7 & J3-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| J7           | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| J3           | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DN1-G60xxN  General 常规型(CANopen & Modbus & Pulse)
J5 & J10-CANopen / Modbus
接口定义

以上媒体的描述: 这张图片显示了一个6针的连接器插座，每个针脚都用数字标记了，从1到6。这个插座通常用于电子设备的连接。

以上媒体的描述: 图片中显示了一个电路板的一部分，标注了两个连接器。左边的连接器标注为“J10: CANopen / Modbus Out”，右边的连接器标注为“J5: CANopen / Modbus In”。这些标注表明了连接器的功能，其中J10用于CANopen或Modbus协议的输出，而J5用于CANopen或Modbus协议的输入。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | ------------ | ---------- | ------------ | ------------ |
| J5 & J10 | CANopen  | 1          | GND          | 黑           |
|              |              | 3          | CANH         | 蓝           |
|              |              | 5          | CANL         | 白蓝         |
|              |  Modbus  | 2          | PE           | 灰           |
|              |              | 4          | MODBUS_485B  | 白黄         |
|              |              | 6          | MODBUS_485A  | 黄           |
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：左侧的矩形框内标注为“CANopen主站”，并且有一个120Ω的匹配电阻。
CANopen节点：中间和右侧的矩形框分别标注为“CANopen节点1”、“CANopen节点2”和“CANopen节点n”，表示多个CANopen节点。
连接线：各个节点之间通过CAN总线连接，标注了CANH、CANL和GND。
PE（保护接地）：每个节点都有一个标注为“PE”的保护接地连接。
匹配电阻：在主站和最后一个节点（节点n）上都有120Ω的匹配电阻。

这个示意图展示了一个典型的CANopen网络拓扑结构，主站和多个节点通过CAN总线连接，并且在网络的两端使用匹配电阻以确保信号完整性。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下内容：

Modbus主站：
标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接到Modbus节点1。

有一个匹配电阻120Ω。


Modbus节点1：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接到Modbus节点2。

有一个接地端子“PE”。


Modbus节点2：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接到Modbus节点n。

有一个接地端子“PE”。


Modbus节点n：

同样标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
有一个匹配电阻120Ω。
有一个接地端子“PE”。

图中展示了Modbus主站和多个Modbus节点之间的连接关系，使用了RS-485通信标准。每个节点之间通过“MODBUS_485A”和“MODBUS_485B”进行数据传输，并且每个节点都有接地端子“PE”以确保安全和稳定的通信。
SW1 & SW2-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| SW1          | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| SW2          | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DM1-G60xxW 双轴 General 常规型（CANopen & Modbus & Pulse）
J5 & J10-CANopen / Modbus
接口定义

以上媒体的描述: 这张图片显示了一个6针的连接器插座。插座的每个针脚都用数字标记，从1到6。插座的右侧有一个黑色的三角形箭头，可能用于指示方向或位置。

以上媒体的描述: 图片中显示了两个接口，分别标记为 "J5: CANopen / Modbus In" 和 "J10: CANopen / Modbus Out"。这些接口用于CANopen和Modbus通信协议，其中J5接口用于输入，J10接口用于输出。旁边还有两个指示灯，分别标记为 "RUN" 和 "ERR"。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | ------------ | ---------- | ------------ | ------------ |
| J5 & J10 | CANopen  | 1          | GND          | 黑           |
|              |              | 3          | CANH         | 蓝           |
|              |              | 5          | CANL         | 白蓝         |
|              |  Modbus  | 2          | PE           | 灰           |
|              |              | 4          | MODBUS_485B  | 白黄         |
|              |              | 6          | MODBUS_485A  | 黄           |
接线示例

以上媒体的描述: 图片中显示了一个CANopen/Modbus通讯型控制器/可编程逻辑控制器的接口面板。面板上有多个接口和端口，标注了不同的功能和连接点。具体包括：

左侧有两个绿色的接线端子，标注为DC-、DC+、PE、U、V、W。
右侧有多个白色和黑色的接口，标注为J4-En1、J7-En2、J8-Brake、J6-Pulse/IO、J3-USB、J5、J10等。
右侧还有一些指示灯和标识，如RUN、ERR、Modbus、CANopen。
图片右下角有文字说明，标注为“CANopen/Modbus通讯型控制器/可编程逻辑控制器”。

这些接口和端口用于连接和控制不同的设备和信号，适用于工业自动化和控制系统。
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：位于左侧，标有“CANopen 主站”。
CANopen节点：有多个节点，分别标有“CANopen 节点1”、“CANopen 节点2”，以及“CANopen 节点n”。
连接线：每个节点之间通过CAN总线连接，标有“CANH”、“CANL”和“GND”。
匹配电阻：在主站和最后一个节点处，各有一个标有“匹配电阻120Ω”的电阻。
PE（保护接地）：每个节点和主站都有一个标有“PE”的保护接地连接。

这个示意图展示了一个典型的CANopen网络拓扑结构，其中主站和多个节点通过CAN总线连接，并在两端使用匹配电阻以确保信号完整性。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下元素：

Modbus主站：
标有“MODBUS_485A”、“MODBUS_485B”和“GND”。
连接了一个120Ω的匹配电阻。

接地（PE）。


Modbus节点：

有多个节点（节点1、节点2、节点n）。
每个节点都标有“MODBUS_485A”、“MODBUS_485B”和“GND”。

每个节点都接地（PE）。


连接线：

各节点之间通过“MODBUS_485A”和“MODBUS_485B”进行连接。

连接线中有波形图示，表示信号传输。


接地（PE）：


每个节点和主站都接地，标有“PE”。


匹配电阻：

主站和最后一个节点（节点n）连接了120Ω的匹配电阻。

这个示意图展示了一个典型的Modbus RS-485通信网络的连接方式，确保了信号的完整性和系统的稳定性。
SW1 & SW2-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| SW1          | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| SW2          | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
DM1-G60xxQ 四轴 General 常规型（CANopen & Modbus & Pulse）
J5 & J10-CANopen / Modbus
接口定义

以上媒体的描述: 这张图片显示了一个6针的连接器插座，每个针脚都用数字标记了，从1到6。这个插座通常用于电子设备的连接。

以上媒体的描述: 图片中显示了两个接口，分别标记为“J5: CANopen / Modbus In”和“J10: CANopen / Modbus Out”。这些接口用于CANopen和Modbus通信协议，其中J5接口用于输入，J10接口用于输出。
| 接口编号 | 接口定义 | 引脚号 | 信号说明 | 线缆颜色 |
| ------------ | ------------ | ---------- | ------------ | ------------ |
| J5 & J10 | CANopen  | 1          | GND          | 黑           |
|              |              | 3          | CANH         | 蓝           |
|              |              | 5          | CANL         | 白蓝         |
|              |  Modbus  | 2          | PE           | 灰           |
|              |              | 4          | MODBUS_485B  | 白黄         |
|              |              | 6          | MODBUS_485A  | 黄           |
接线示例

以上媒体的描述: 这张图片展示了一个多轴控制器的面板。面板上有四个轴（Axis1到Axis4），每个轴都有多个接口和端口，包括：

DC+、DC-、PE、U、V、W端子，用于电源和电机连接。
J6-Pulse/I/O、J4-En1、J7-En2、J8-Brake、J5、J10、J3-USB等接口，用于脉冲输入/输出、使能信号、刹车信号、CANopen/Modbus-RTU通信和USB连接。

右下角的注释框中写有“CANopen/Modbus通讯型控制器/可编程逻辑控制器”，表明该设备支持CANopen和Modbus-RTU通信协议，并且可能具有可编程逻辑控制功能。
在使用多电源且不共电源 GND 的系统中，非同一电源拖动的设备之间若要进行 CANopen / Modbus 通讯，必须增加隔离设备，防止烧毁硬件。
多节点链接方式推荐使用“手牵手”式连接，如下图所示；如需使用“T”型（桩线）分支连接，分支桩线的长度建议不要超过 1m；不推荐使用星型连接。

更多详细说明请查阅硬件抗干扰指导

CANopen 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个CANopen网络的连接示意图。图中包含以下元素：

CANopen主站：位于左侧，标有“CANopen 主站”。
CANopen节点：有多个节点，分别标有“CANopen 节点1”、“CANopen 节点2”以及“CANopen 节点n”。
CAN总线连接：每个节点之间通过CAN总线连接，标有“CANH”、“CANL”和“GND”。
匹配电阻：在主站和最后一个节点处，各有一个标有“匹配电阻120Ω”的电阻。
PE连接：每个节点和主站都有一个标有“PE”的连接，表示保护接地。

这个示意图展示了一个典型的CANopen网络拓扑结构，其中主站和多个节点通过CAN总线连接，并在两端使用匹配电阻以确保信号完整性。
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下元素：

Modbus主站：
标记为“Modbus 主站”。
包含MODBUS_485A、MODBUS_485B和GND连接。
连接了一个匹配电阻120Ω。

连接到PE（保护接地）。


Modbus节点：

有多个节点，标记为“Modbus 节点1”、“Modbus 节点2”以及“Modbus 节点n”。
每个节点包含MODBUS_485A、MODBUS_485B和GND连接。

每个节点都连接到PE（保护接地）。


连接线：

各节点之间通过MODBUS_485A、MODBUS_485B和GND连接线连接。

连接线中有波形图示，表示信号传输。


匹配电阻：

在Modbus主站和最后一个Modbus节点（Modbus 节点n）上各有一个匹配电阻120Ω。

这个示意图展示了一个典型的Modbus RS-485通信网络的布线和接地方式。
SW1 & SW2-终端电阻的使用
| 接口编号 | 接口定义             | 说明                                     |
| ------------ | ------------------------ | -------------------------------------------- |
| SW1          | CANopen 总线通讯终端电阻 | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
| SW2          | Modbus 总线通讯终端电阻  | 120 欧姆电阻（有跳线帽时为有效接入终端电阻） |
注意在布线的时候电阻和电抗分配必须合理，当通讯传输距离过长时，需要在首尾两端各配一个 120 欧的终端电阻，不可只接单端或不接，且只能在两端接，中间节点不接。
Dservo-脉冲型
J1 & J2-Modbus
接口定义

以上媒体的描述: 图片中有两个插头的示意图。左边的是一个插头的正面视图，右边的是一个插座的正面视图。两个示意图中都标注了“1pin位置”，指示了插头和插座中第一个引脚的位置。
| 接口编号 | 接口定义 | 引脚号 | 标签      |
| ------------ | ------------ | ---------- | ------------- |
| J1 & J2  | Modbus   | 1          | Not Used      |
|              |              | 2          | Not Used      |
|              |              | 3          | MODBUS_485_2A |
|              |              | 4          | GND           |
|              |              | 5          | GND           |
|              |              | 6          | MODBUS_485_2B |
|              |              | 7          | USER_CANH     |
|              |              | 8          | USER_CANL     |
Modbus 主站线性拓扑结构

以上媒体的描述: 这张图片展示了一个Modbus通信网络的连接示意图。图中包含以下内容：

Modbus主站：位于左侧，标有“Modbus 主站”。
Modbus节点：有多个节点，分别标有“Modbus 节点1”、“Modbus 节点2”以及“Modbus 节点n”。
连接线：各节点之间通过Modbus_485A、Modbus_485B和GND（地线）连接。
匹配电阻：在Modbus主站和最后一个Modbus节点上都有标注“匹配电阻120Ω”。
PE（保护接地）：每个节点和主站都有一个PE连接点。

这个示意图展示了一个典型的Modbus RS-485总线网络的连接方式，确保了各节点之间的通信和信号完整性。